# MongoDB Connection String
# For local MongoDB (MongoDB installed on your computer):
DBURL=mongodb://localhost:27017/auth-app

# For MongoDB Atlas (cloud):
# DBURL=mongodb+srv://username:password@cluster.mongodb.net/auth-app?retryWrites=true&w=majority

# JWT Secret Key (change this to a random string)
JWT_SECRET=my_super_secret_jwt_key_change_this_in_production

# Server Port
PORT=5000

# Environment
NODE_ENV=development
